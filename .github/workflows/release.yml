name: Build Electron App (macOS & Windows)

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:

jobs:
  build:
    name: Build for ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macos-latest, windows-latest]

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - run: npm ci
      - run: npm run build

      # 🧱 Build app but skip any publishing
      - name: Build for macOS
        if: matrix.os == 'macos-latest'
        run: npx electron-builder --mac --publish=never

      - name: Build for Windows
        if: matrix.os == 'windows-latest'
        run: npx electron-builder --win --publish=never

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.os }}
          path: release/

  release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Download mac artifact
        uses: actions/download-artifact@v4
        with:
          name: macos-latest
          path: dist/mac

      - name: Download win artifact
        uses: actions/download-artifact@v4
        with:
          name: windows-latest
          path: dist/win

      # ✅ Create GitHub release manually using Actions token (no GH_TOKEN needed)
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: "Release ${{ github.ref_name }}"
          files: |
            dist/mac/**
            dist/win/**
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
name: Build Electron App (macOS & Windows)

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:

jobs:
  build:
    name: Build for ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macos-latest, windows-latest]

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - run: npm ci
      - run: npm run build

      # 🧱 Build app but skip any publishing
      - name: Build for macOS
        if: matrix.os == 'macos-latest'
        run: npx electron-builder --mac --publish=never

      - name: Build for Windows
        if: matrix.os == 'windows-latest'
        run: npx electron-builder --win --publish=never

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.os }}
          path: release/

  release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Download mac artifact
        uses: actions/download-artifact@v4
        with:
          name: macos-latest
          path: dist/mac

      - name: Download win artifact
        uses: actions/download-artifact@v4
        with:
          name: windows-latest
          path: dist/win

      # ✅ Create GitHub release manually using Actions token (no GH_TOKEN needed)
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: "Release ${{ github.ref_name }}"
          files: |
            dist/mac/**
            dist/win/**
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
