# Basic Electron + Vite + React (TypeScript)

## Run dev
1. npm install
2. npm run dev:all

This will run Vite and compile electron main in watch mode, then launch Electron when Vite is ready.

## Build
1. npm run build
2. npm run start:electron
